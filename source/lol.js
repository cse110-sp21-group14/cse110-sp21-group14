/**
 * lol test function haha
 * @param {number} p1 the first num
 * @param {number} p2 the second num
 * @returns {number} output lol
*/
function myFunction(p1, p2) {
        return p1 * p2;    // The function returns the product of p1 and p2
}

myFunction(1,2);